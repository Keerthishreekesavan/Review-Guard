server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Hand off routing to the React SPA
        try_files $uri $uri/ /index.html;
    }
}
